#include<iostream>
#include<vector>
#include<algorithm>
using namespace std;
int main(){
    string s;
    cin>>s;
    cout<<s<<endl;
    next_permutation(s.begin(),s.end());
    cout<<s<<endl;
    next_permutation(s.begin(),s.end());
    cout<<s<<endl;
    next_permutation(s.begin(),s.end());
    cout<<s<<endl;
    next_permutation(s.begin(),s.end());
    cout<<s<<endl;
    next_permutation(s.begin(),s.end());
    cout<<s<<endl;
    next_permutation(s.begin(),s.end());
    cout<<s<<endl;
    next_permutation(s.begin(),s.end());
    cout<<s<<endl;
    next_permutation(s.begin(),s.end());
    cout<<s<<endl;
    next_permutation(s.begin(),s.end());
    cout<<s<<endl;
    next_permutation(s.begin(),s.end());
    cout<<s<<endl;
    next_permutation(s.begin(),s.end());
    cout<<s<<endl;
    next_permutation(s.begin(),s.end());
    cout<<s<<endl;
    next_permutation(s.begin(),s.end());
    cout<<s<<endl;
    next_permutation(s.begin(),s.end());
    cout<<s<<endl;
    next_permutation(s.begin(),s.end());
    cout<<s<<endl;



    return 0;




}