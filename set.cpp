#include<iostream>
#include<set>
int main(){
    // set contains unique elements only
    // no moidficatiom are allowed
    set<int>s;
    s.insert(5);
    s.insert(4);
    s.insert(3);
    s.insert(2);
    s.insert(1);
}